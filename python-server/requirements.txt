# ============================================================
# llm-wiki Python 后端（Spec 038）
#   pip install -r requirements.txt
# 说明：LLM 请求层通过 Agently 框架对接 OpenAI 兼容端点；
#       Embedding / Rerank 仍通过 httpx 直连。
# ============================================================

# --- FastAPI 骨架 ---
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
httpx>=0.28

# --- LLM 请求层 ---
agently>=4.1

# --- 索引与检索 ---
jieba>=0.42
tantivy>=0.20 # BM25 全文检索（Rust 内核，替换原自研引擎）
lancedb>=0.20
pyarrow>=18
numpy>=2.1

# --- 测试 ---
pytest>=8.3
pytest-asyncio>=0.24
